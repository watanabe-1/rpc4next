{
  "name": "rpc4next-monorepo",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/watanabe-1/rpc4next.git"
  },
  "workspaces": [
    "packages/*",
    "integration/*"
  ],
  "engines": {
    "node": ">=20.19.2"
  },
  "scripts": {
    "build": "bun --filter \"./packages/*\" --if-present build",
    "clean": "bun --filter \"./packages/*\" --if-present clean",
    "typecheck": "bun --filter \"./packages/*\" --if-present typecheck",
    "lint": "bun --filter \"./packages/*\" --if-present lint",
    "lint:fix": "bun --filter \"./packages/*\" --if-present lint:fix",
    "lint:repo": "biome check .",
    "lint:biome:demo": "biome check --config-path biome.demo.json biome/tests",
    "lint:repo:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage.enabled true",
    "test:ui": "vitest --ui --coverage.enabled true",
    "test:watch": "vitest --watch",
    "integration:next-app:generate": "bun run --cwd integration/next-app generate:rpc",
    "integration:next-app:watch": "bun run --cwd integration/next-app generate:rpc:watch",
    "integration:next-app:test": "bun run --cwd integration/next-app test",
    "integration:next-app:typecheck": "bun run --cwd integration/next-app typecheck",
    "integration:next-app:dev": "bun run --cwd integration/next-app dev",
    "integration:next-app:build": "bun run --cwd integration/next-app build:app",
    "integration:next-app:smoke": "bun run --cwd integration/next-app smoke:client",
    "diff2prompt": "diff2prompt --excludeFile=.gitignore"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.5",
    "@types/node": "^25.3.3",
    "@vitest/coverage-v8": "^4.0.18",
    "@vitest/ui": "^4.0.18",
    "diff2prompt": "^0.0.9",
    "esbuild": "^0.27.3",
    "fast-glob": "^3.3.3",
    "mock-fs": "^5.5.0",
    "msw": "^2.12.10",
    "next": "16.1.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18",
    "zod": "^4.3.6"
  }
}
