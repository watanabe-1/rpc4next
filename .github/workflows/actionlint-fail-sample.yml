name: Actionlint Fail Sample

on:
  workflow_dispatch:

jobs:
  fail-case:
    # Intentional typo for actionlint test.
    runs-on: ubunut-latest
    steps:
      - name: Invalid uses format
        # Missing @ref on purpose.
        uses: actions/checkout

      - name: Broken expression
        if: ${{ startsWith(github.ref, 'refs/heads/main') && }}
        run: echo "This is an intentional actionlint failure sample"
